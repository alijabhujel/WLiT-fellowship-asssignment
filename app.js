function multiplication(number, r) {
    let table = '';
    for (let i = 1; i <= r; i++) {
        table += `${number} x ${i} = ${number * i}\n`;
    }
    return table;
}
const number = 5; 
const r = 10; 
console.log(multiplication(number, r));


function check(number,r){
    let result = '';
    if(number > 0 ){
        
    }
}